/**
 * 빈 시작 화면에서 한 줄씩 돌아가는 가이드 팁.
 *
 * 화면이 스스로 쓰임새를 말하는 자리다 — 처음 여는 사람은 화면을 캡처해
 * 붙일 수 있다는 것도, 카드를 눌러 질의 대상으로 담을 수 있다는 것도
 * 모른다. 예시 질문 chip 이 "무엇을 물을까"를 보여준다면, 팁은 "이 도구로
 * 무엇을 할 수 있나"를 알려준다.
 *
 * **문구를 늘리려면 이 배열에 한 줄 더하면 된다** — 순서대로 돌고, 고칠
 * 곳은 여기뿐이다. 규칙은 세 가지이고 `startTips.test.ts` 가 지킨다:
 *
 * 1. 한 문장, `TIP_MAX_LENGTH` 자 이내 — 좁은 화면에서 두 줄로 접히면 한
 *    줄 팁이 아니게 된다.
 * 2. 중복 금지 — 같은 문구가 두 번 돌면 회전이 멈춘 것처럼 보인다.
 * 3. 화면에 실제로 있는 동작만 — 없는 기능을 안내하는 팁은 거짓말이 된다.
 */
export const START_TIPS: readonly string[] = [
  "현재 보고 있는 화면을 캡처해서 이미지로 붙여넣어 보세요.",
  "\"설비에 이상이 있나요?\" 처럼 편하게 물어보셔도 됩니다.",
  "설비 카드를 선택하면 그 설비가 질문의 대상이 됩니다.",
  "질문에 데이터가 필요하면 요청 카드로 청합니다 — 조회 결과를 붙여넣어 주세요.",
  "등록한 데이터는 다음 질문에 함께 실려 나갑니다.",
] as const;

/**
 * 다음 문구로 넘어가는 간격. 한 줄을 읽고 잠깐 여유가 남는 길이 —
 * 더 짧으면 읽는 중에 바뀌고, 더 길면 두 번째 문구를 못 보고 떠난다.
 */
export const START_TIP_INTERVAL_MS = 6000;

/** 한 줄로 남기 위한 상한 — 768px 채팅 폭에서 caption 한 줄에 들어가는 길이. */
export const TIP_MAX_LENGTH = 45;
